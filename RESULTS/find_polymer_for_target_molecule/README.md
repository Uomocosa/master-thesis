All resutls obtained from the `bio.find_polymer_for_target_molecule` function
Each subfolder corresponds to the last result of `pixi run find_polymer_for_target_molecule <target_molecule>`. It contains:
- The config.yaml of the function `bio.find_polymer_for_target_molecule`.
- The varius csv are rulst from the many filtering. It start from the `01_generated_polymers.csv` which are generated form the `PeeSmileGenerator` mingpt wrapper.
- Finally it has two plots that represent the HIGHEST-CAPACITY polymer predicted for the `<target_molecule>`
