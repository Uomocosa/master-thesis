The PeeSmileCapacityPredictor is a simple MLP model using the best hyperpyrameters found by the `bio.mlp_experiment.run_all_experiments` function.
Specifically, ranked by the `bio.mlp_experiment.run_all_experiments.py::rank_experiments` function.

We also plotted the learning plot for this model.
